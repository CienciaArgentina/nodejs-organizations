export enum Paths {
    Get = '/organizations',
    GetById = '/organizations/:id',
    Post = '/organizations',
    Patch = '/organizations/:id',
    PostProfile = '/organizations/:id/user-profiles',
}